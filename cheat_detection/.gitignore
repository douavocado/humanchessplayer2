# Engine-analysis cache (regenerated on demand, can be large)
cache/
# Generated human baselines and reports
baselines/
*.report.json
report.md
report.json
# Downloaded/filtered human corpora (can be large)
corpora/
# Orchestrator working dir (fetched games, reports) -- except the
# breadth-sweep driver script itself, which is code, not a generated output.
runs/*
!runs/breadth_sweep/
runs/breadth_sweep/*
!runs/breadth_sweep/run_sweep.py
