{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.py) \"$CLAUDE_PROJECT_DIR\"/venv/bin/python -m py_compile \"$f\" || exit 2;; esac; }",
            "timeout": 15,
            "statusMessage": "Syntax-checking edited Python file"
          }
        ]
      }
    ]
  }
}
